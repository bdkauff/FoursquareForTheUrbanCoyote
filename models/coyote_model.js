var mongoose = require('mongoose');
var Schema = mongoose.Schema;

// define a new schema
var coyoteSchema = new Schema({
	// lat/long
	// time stamp
	// other data



});
module.exports = mongoose.model('coyote',richterSchema);